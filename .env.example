# Copy this file to .env for local development (never commit .env)

# Paste the full contents of your Google service account JSON here (single line).
# If set, users don't need to paste credentials in the UI.
GOOGLE_SERVICE_ACCOUNT_JSON=

# Optional: override default port
PORT=8000
